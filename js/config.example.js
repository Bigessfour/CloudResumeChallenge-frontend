/**
 * Runtime config (generated in CI or copied from config.example.js for local dev).
 * Do not commit js/config.js — it is created by the deploy workflow.
 */
window.VISITOR_API_CONFIG = {
  url: "",
};
