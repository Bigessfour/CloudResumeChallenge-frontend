/**
 * Cloud Resume Challenge — app initialization.
 * Add Syncfusion EJ2 controls here as you build out the UI.
 */
(function () {
  "use strict";

  document.addEventListener("DOMContentLoaded", function () {
    // Example: initialize controls when added via MCP / Agent
    // if (document.getElementById("experience-grid") && ej.grids) {
    //   new ej.grids.Grid({ /* ... */ });
    // }
  });
})();
